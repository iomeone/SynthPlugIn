/*
  ==============================================================================

	GlobalConstants.h
	Created: 26 Oct 2018 2:15:30am
	Author:  Mike

  ==============================================================================
*/

#pragma once

const int componentWidth = 50; //sets width of components
const int componentHeight = 60; //sets height of components
const int textBoxWidth = 40; //sets width of numeric text box for sliders
const int textBoxHeight = 14; //sets height of numeric text box for sliders
const int componentMargin = 3; //sets margin for inner padding of components

const int screenWidth = 1080; // sets width of window
const int screenHeight = 500; // sets height of window

const int labeledGroupHorizontalMargin = 8; // sets horizontal margin for labeledGroups
const int betweenLabeledComponentMargin = 2; // sets margin between labeledComponents

const int verticalLabeledGroupMargin = 28; // sets vertical margin for labeledGroups (/2)
const int verticalLabeledComponentTranslation = 3; //vertically translates the labeledComponents to compensate for different header font sizes
const int outerScreenMargin = 5; //sets margin around edge
const int betweenLabeledGroupMargin = 2; //sets margin between labeledGroups

const int scrollBarWidth = 8; //default is 8 according to documentation